### Open a terminal tab and run the following commands:

# run lnd on bitcoin network, testnet, using neutrino
./lnd --bitcoin.active --bitcoin.testnet --debuglevel=info --bitcoin.node=neutrino --neutrino.connect=faucet.lightning.community


### Open a new terminal window

pip install lnd-grpc


### (Optional) Make a new wallet using the CLI

cd lnd-linux-amd64-v0.6.1-beta

./lncli create

Enter a password, this is for testnet coins, so you can use an insecure password, 8 character minimum



### To start over:


Open a new terminal tab

cd lnd-linux-amd64-v0.6.1-beta`

./lnd --bitcoin.active --bitcoin.testnet --debuglevel=info --bitcoin.node=neutrino --neutrino.connect=faucet.lightning.community

Open a new terminal tab

cd lnd-linux-amd64-v0.6.1-beta

./lncli unlock

Enter your password and hit enter
